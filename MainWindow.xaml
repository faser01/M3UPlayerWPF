<Window x:Class="M3UPlayerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:M3UPlayerWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <ListBox x:Name="listBoxChannels" HorizontalAlignment="Left" Margin="10" Width="300"/>

        <Button x:Name="btnPlay" Content="Play" HorizontalAlignment="Center" Margin="0,0,0,10" VerticalAlignment="Bottom" Width="75" Height="25" Click="btnPlay_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#FF007ACC" BorderBrush="#FF007ACC" BorderThickness="1" CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <!-- Элемент для воспроизведения медиафайлов -->
        <MediaElement x:Name="MediaElementPlayer" Width="480" Height="270" HorizontalAlignment="Right" Margin="0,50,10,0" VerticalAlignment="Top"/>
    </Grid>
</Window>

